# 生产更新状态（更新至 2026-08-25）

## 当前发布节点

- 状态：功能开发、生产更新包和隔离升级/回滚演练均已完成，待提交交付。
- 分支：`feat/temporal-pairwise-state`
- 开发起点提交：`59f1ec7`
- 生产安装版：`CellVision-offline-6231fdce81-cpu`（仅作发布记录，不作为升级程序的校验或安装前置条件）。

## 本节点已完成

1. 审核入口新增可编辑的孔级前置筛选：
   - 末点细胞覆盖率严格大于输入值；
   - Day2 杂质数严格小于输入值；
   - Day0、Day1、Day2 细胞数分别位于各自输入的闭区间；
   - 任一条件留空表示不限；筛选只影响审核列表，不修改模型结果；
   - 在板子列表顶部统一设置，不再逐板弹窗；显示任务总命中孔数及每块板命中孔数；
   - 进入单块板后仍可修改本板条件，并显示本板命中孔数；
   - 审核摘要版本升级到 v4，旧缓存自动失效，避免新增指标缺失导致筛选结果为 0。
2. 按孔快速审核新增人工判定标签：
   - `Q`：合格（`approved`）；
   - `W`：待定（`pending`）；
   - `E`：排除（`rejected`）；
   - 默认状态为未判定（`unclassified`），不会计入上述三类；只有人工操作后才进入统计，并可清除标签恢复未判定；
   - 使用现有 `well_screening_reviews` 持久化，兼容离线审核结果导入/导出。
3. 项目入口新增人工判定统计：
   - 板子列表上方显示当前任务合格、待定、排除孔总数；
   - 每块板“结果汇总”中显示该板合格、待定、排除孔数。
4. 审核界面新增按时间点人工修正细胞总数：
   - T0、T1、T2 卡片顶部显示“细胞总数”，左侧减一、右侧加一；
   - 细胞总数位于卡片下方控制行左侧，与右侧缩放比例及视图按钮同一横行，不再堆叠在其上方；
   - 自动总数按单细胞=1、粘连双细胞=2、3+细胞团=3计算；
   - 未进行总数人工修改时，更改目标类型会立即自动重算该时间点总数；
   - 使用加减按钮后切换为“人工”总数，后续目标类型变化不会覆盖人工值；
   - “恢复自动”会清除人工值并重新跟随目标类型计算；
   - 加减与恢复自动采用前端即时更新，350ms 内连续操作合并为一次后台保存；保存报告期间不再锁住加减按钮；
   - 人工总数随数据库及离线审核结果导入/导出持久化。
5. Excel 报告结构调整：
   - `孔结论`后新增`人工判定`列，显示合格/待定/排除；未判定孔留空；
   - 不再导出每个时间点的单细胞、粘连双细胞、细胞团分项个数；
   - 仅保留`T0细胞总数`、`T1细胞总数`、`T2细胞总数`；人工总数优先；
   - 新增`末点细胞覆盖率`和`Day2杂质数`。

## 本节点验证

- 细胞总数、离线同步、Excel及审核界面针对性测试：43 passed。
- 完整测试集 `pytest -q`：245 passed；仅有既有 FastAPI `on_event` 弃用警告。
- `python -m compileall -q src`：通过。
- `node --check review-ui/auto-review.js`：通过。
- `node --check review-ui/project-dashboard.js`：通过。
- `git diff --check`：通过。
- 2026-08-24 源码服务验证：安装版服务占用 `8777` 且当前会话无管理员停服权限，因此源码服务启动于 `http://127.0.0.1:8778/`；`/api/ready` 为 `ready`。真实项目共有 861 个可审核孔，示例筛选命中 741 个；T1-1 板 63 个可审核孔均返回完整筛选指标，默认全部为未判定。
- 浏览器真实页面冒烟：项目顶部显示三类孔统计及统一筛选；T1-1/A2 的 T0/T1/T2 分别显示自动总数 1/1/4，加减与恢复自动控件状态正确；Day7/Day14 不显示该控件；孔人工判定按钮载入后可用；页面无 console error/warning。
- 细胞总数布局/性能复核：在 1280px 浏览器宽度下，细胞总数位于控制行左侧，缩放与视图按钮位于右侧，两组居中对齐、互不重叠且均在卡片内；连续“＋/−/恢复自动”即时显示 1→2→1→自动1，仅产生合并后的后台保存，最终数据库状态恢复自动，页面无 console error/warning。
- 2026-08-25 完整测试集：247 passed；仅有既有 FastAPI `on_event` 弃用警告。
- 2026-08-25 浏览器真实页面复核：1280px 下统一筛选栏保持单行，Day0/Day1/Day2 范围输入框约 54px；Day0≥1 且 Day1≤5 时任务统计为 832/861 孔，进入 T1-1 后条件正确继承并命中 58/63 孔，页面无 console error/warning。

## 2026-08-25 增量修改

1. 新建任务改为按所选时间点取完整板子交集：
   - 只有 Day0、Day1、Day2 和所选末点均存在且96孔文件完整的板子进入任务；
   - 缺少任一时间点或对应文件夹不完整的板子自动排除，不再导致整个任务创建失败；
   - 解析结果和任务记录均保存纳入板数、排除板数及逐板原因；如果交集为0，仍会明确阻止创建；
   - 交集板号同时传给任务工作进程和末点快速筛选，确保排除板不进入任何计算阶段。
2. 统一筛选和单板筛选均新增 Day0、Day1 细胞数闭区间；Day2 条件保持不变。
3. 项目筛选栏改为紧凑弹性布局；1280px 宽度保持单行，更窄页面自动换行。

## 明确延期（不进入本节点）

1. 单细胞/双细胞判定逻辑与模型优化。
2. 杂质判定模型优化，包括白色小点等不应视为杂质的情形。

以上两项涉及模型、训练数据和验证集，不在本次界面与导出更新中修改。

## 2026-08-25 日常启动入口与项目可见性诊断

1. 新增便携生产版日常入口 `Start-CellVision.cmd` / `start_cellvision_platform.ps1`：
   - 不执行运行环境配置，不重装服务，不修改 Workspace 权限；
   - 服务已运行时直接复用；服务停止时才申请管理员权限并启动；
   - 校验 Windows 服务是否属于当前安装目录，避免从另一份发布目录启动后看到不同 Workspace；
   - 同时检查 `/api/ready` 和计算工作器状态，通过后再打开桌面桥接入口；
   - 强制刷新项目目录数据库，并检查直接子目录中的 `project.json` 是否存在且可解析；
   - 日志写入 `Workspace/Logs/cellvision-daily-launch.log`。
2. 项目文件夹仍在但主页不可见的直接原因已确认：
   - 首页和目录数据库以 `Workspace/Projects/<项目>/project.json` 为项目入口；仅保留 `plates`、报告或图片目录不能被识别为项目；
   - 服务重启后会重新核对磁盘清单，缺失清单的数据库项目会标记为删除并从主页过滤，但板子结果文件不会因此被删除；
   - 如果服务被另一份安装目录重新注册，也会读取另一份 Workspace；日常入口会拒绝这种目录不一致并显示双方路径；
   - 当前本机只读验证识别到 `ql2603-production-acceptance-6231fdce81` 目录缺少 `project.json`，与“文件夹仍在但主页不可见”的现象一致，未自动重建或修改该项目数据。
3. 已增加项目元数据保护与安全恢复：
   - 每次执行服务配置前，将所有项目的 `project.json`、任务队列、任务计划及一致性 SQLite 目录快照备份到 `Workspace/Projects/.metadata-backups/<时间戳>/`；
   - 同一安装目录下重复配置时只复用并重启现有服务，不再卸载重装；检测到服务属于另一安装目录时默认拒绝切换，必须明确使用 `-RepairService` 才允许重新注册；
   - 启动时优先从最新有效备份原样恢复缺失清单；没有备份时，仅在目录数据库记录对应的每块板均保留产物目录、配置、图片清单和报告时才重建清单；
   - 对缺失关键产物的残缺项目只报告原因并跳过，不删除原目录，也不制造无法正常打开的项目；恢复后强制刷新目录数据库，使曾被标记删除的有效项目重新显示。
4. 当前本机的 `ql2603-production-acceptance-6231fdce81` 只剩一个 `annotations.db`，清单、配置、图片清单、报告及两块板目录均缺失，无法从现存文件安全重建，工具已按设计跳过且未改动原数据。生产电脑上的项目如果板子产物完整或已有新备份，则可自动恢复；否则需要找回原 `project.json` 或对应备份。
5. 验证：Windows PowerShell 5.1 语法解析通过；服务已运行场景下日常入口成功，API、工作器、目录数据库刷新和异常目录提示均正常；元数据精确恢复、完整目录重建、残缺项目拒绝恢复等针对性测试通过；完整测试集 `pytest -q`：`250 passed`，仅有既有 FastAPI `on_event` 弃用警告；`git diff --check` 通过。
6. 新增生产端手动恢复入口 `Recover-CellVision-Projects.cmd` / `recover_cellvision_projects.ps1`：
   - 放在安装目录根目录双击执行；先生成元数据备份，再执行安全恢复并确保服务/计算工作器可用，最后强制刷新主页目录；
   - 控制台逐项显示从备份恢复、从完整产物重建及跳过的项目与原因；完整日志写入 `Workspace/Logs/cellvision-project-recovery.log`；
   - 独立恢复包 `deploy/updates/CellVision-ProjectRecovery-20260825.zip` 内含入口、辅助脚本及中文说明，生产端无需联网或安装额外依赖。

## 生产更新包 r1（历史）

- 包名：`CellVision-Update-20260824-r1.zip`
- 路径：`deploy/updates/CellVision-Update-20260824-r1.zip`
- SHA-256（仅用于交付记录，不参与安装校验）：`20877bf0c280869feff7d35518dc565858889e7dd07220c347d8b8e8addfa038`
- 覆盖范围：15 个审核界面和服务端运行文件；不覆盖 Workspace、数据、模型、Python 环境及生产配置。
- 安装方式：解压 ZIP，将整个 `CellVision-Update-20260824-r1` 文件夹放到安装目录根目录，双击 `Update-CellVision.cmd` 并允许管理员权限。
- 安装行为：自动定位 `CellVisionProduction` 服务，先将全部待覆盖文件备份到应用目录的 `update-backups`，再直接覆盖文件、重启服务并等待 `/api/ready` 返回 `ready`。
- 校验策略：按要求，不校验生产基线，也不校验每个文件哈希；目标文件不存在时停止，避免误装到错误目录。
- 自动恢复：覆盖、服务启动或健康检查失败时，先停止服务并自动恢复本次备份，再按更新前的服务状态恢复运行。
- 手工回滚：双击同目录的 `Rollback-CellVision.cmd`，恢复最近一次更新备份并重启服务。
- 隔离演练：从 ZIP 解压，以生产版 15 个文件构造临时安装副本；PowerShell 7 和 Windows PowerShell 5.1 下的升级、内容核对、备份、回滚及生产原文件恢复均通过。
- 生产状态复核：演练未修改真实生产目录；`CellVisionProduction` 保持 `Running`，`http://127.0.0.1:8777/api/ready` 返回 `ready`。

## 生产更新包 r2（当前）

- 包名：`CellVision-Update-20260825-r2.zip`
- 路径：`deploy/updates/CellVision-Update-20260825-r2.zip`
- SHA-256（仅用于交付记录，不参与安装校验）：`da4a074dcb6aa49b0c43a63a7554bb4e4cf3c4694d7bd8fbd8b6e63f48dea442`
- 覆盖范围：累计覆盖17个运行文件，包含 r1 全部修改，以及任务工作进程和末点筛选脚本；不覆盖 Workspace、数据、模型、Python 环境及生产配置。
- 安装方式：解压 ZIP，将整个 `CellVision-Update-20260825-r2` 文件夹放到安装目录根目录，双击 `Update-CellVision.cmd` 并允许管理员权限。
- 校验策略：按要求，不校验生产基线，也不校验每个文件哈希；安装前备份17个目标文件，失败时自动恢复。
- 回滚方式：双击同目录的 `Rollback-CellVision.cmd`，恢复最近一次更新备份并重启服务。
- 隔离演练：最终 ZIP 在 Windows PowerShell 5.1 下完成17个生产原文件升级、逐字节核对、备份、回滚及生产原文件恢复，全部通过。

## 发布复核清单

- 在生产模式打开一个真实项目，确认板子列表顶部可统一设置条件、任务与单板命中数同步刷新，进入板子时不再弹窗。
- 进入单块板后修改筛选条件，核对本板命中数与孔列表同步变化。
- 分别用 `Q/W/E` 保存三个孔，返回项目入口核对任务总数和单板结果汇总。
- 更改一个未人工修正总数的目标类型，核对总数自动重算；再用加减按钮设为人工值，继续改类型时核对人工值保持；点击恢复自动后核对重新计算。
- 导出 Excel，核对仅含三个时间点总数、人工判定、末点覆盖率和 Day2 杂质数，不再含三类细胞分项列。
- 验证离线审核包保存标签及人工细胞总数、导出结果并导回生产端。
- 记录最终提交号、更新包路径、SHA-256、安装步骤和回滚步骤。
