# Cell Vision 单细胞识别模型 — 完整逻辑梳理

## 一、项目概览

**Cell Vision** 是一个用于 96 孔板单细胞图像自动识别的深度学习系统。它的核心任务是：读取 96 孔板在不同时间点（T0-T4，对应培养天数 D0、D1、D2、D14）拍摄的单孔显微照片，自动区分 **活细胞** 与 **杂质/碎片/孔壁伪影**，并追踪同一细胞跨时间点的变化。

系统采用 **"弱监督→强监督"渐进式训练策略**，结合传统计算机视觉方法与现代深度学习，最终形成了一个包含多个专用神经网络的 V2 管道。

---

## 二、整体数据流（Pipeline 架构）

```
原始数据 (每个孔 T0-T4 的 TIFF 图像 + 仪器 CF 掩膜)
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Phase 1: 数据准备                                        │
│  [audit] → [build-manifest] → [split] → [baseline]       │
│  审计硬件   构建图像索引        划分训练集   提取CF分量     │
└──────────────────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Phase 2: 候选生成 + 伪标签（基础层）                       │
│  [dense_candidates] → [pseudo_labels] → [hierarchy]      │
│  多尺度峰值检测      形态学伪标签        跨源去重整合       │
└──────────────────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Phase 3: V1 弱监督训练 + 教学标注                         │
│  [train weak-segmenter] → [teaching] → [multiplicity]    │
│  TinyUNet                 ResNet-18    单/双/团簇分类     │
└──────────────────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Phase 4: V2 强监督训练（核心模型）                         │
│  [train v2-instance] → [train v2-temporal]               │
│  SeededInstanceUNet      TemporalEvidenceNet             │
└──────────────────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Phase 5: V2 推断 + 决策                                  │
│  [infer-v2] → [infer-v2-temporal] → [evaluate]           │
│  实例分割掩膜   时间证据调整概率    孔级生长筛选             │
└──────────────────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Phase 6: 人机协同审核 (Human-in-the-loop)                 │
│  [review server] — Web UI 多个审核页面 + SQLite 持久化     │
└──────────────────────────────────────────────────────────┘
```

---

## 三、核心模块详解

### 3.1 候选生成：`dense_candidates.py` — 高召回率峰值检测

这是整个管道最基础的一步，目标是 **尽可能多地找到图像中的"潜在物体"**（宁可多找，不可漏掉）。

#### 3.1.1 多尺度响应图构建

对每张原始灰度图像：
```python
fine       = gaussian_filter(raw, σ=1.2)     # 细尺度
background = gaussian_filter(raw, σ=12.0)    # 粗尺度（背景估计）
medium     = gaussian_filter(raw, σ=4.0)     # 中尺度
response   = max(background - fine, 0.7 * |medium - fine|)
```

- `background - fine`：背景减除，突出暗色物体（细胞在明场下呈暗色）
- `|medium - fine|`：多尺度对比度增强
- 加权合并得到最终的响应图

#### 3.1.2 动态孔壁检测

每个孔的孔壁位置不同（因拍摄略有偏移），系统会自动检测每张图像的孔壁内缘：

```python
# 计算径向梯度剖面
radial_gradient = |∇·(径向单位向量)|
# 在 r ∈ [0.38, 0.49] × min(width, height) 范围内搜索
# 找到径向梯度突然升高的位置（即孔壁开始处）
# 找到第一个"持续上升段"作为 wall_inner_fraction
```

检测到第一个 **连续 3 个 bin 超过阈值** 的上升段作为孔壁内缘。如果检测不到，回退到默认值 0.44。

#### 3.1.3 三区域分区峰值检测

图像被分成三个同心区域，各自独立分配候选配额：

| 区域 | 径向范围 | 配额 | 说明 |
|------|---------|------|------|
| **孔内区域** (interior) | r < wall_inner - buffer | 主配额 (约78%) | 细胞主体区域 |
| **壁缓冲区** (wall buffer) | wall_buffer_start ~ wall_inner | 16% 配额 | 靠近孔壁，可能有细胞贴壁 |
| **壁救援区** (wall rescue) | wall_inner ~ wall_inner+0.03 | 6% 配额 | 细胞中心在孔壁上，用 Hessian blobness 过滤弧形伪影 |

每个区域使用百分位阈值 + 网格覆盖策略：
- 先用 `response_percentile`（~98%）确定响应阈值
- 再用 `minimum_response` 绝对值作为底线（防止空视野出峰）
- 用 8×8 网格保证空间覆盖的均匀性

#### 3.1.4 极坐标壁残差检测（wall_residual）

这是处理"细胞重叠在孔壁上"这一困难场景的关键创新：

```
原始图像 → 极坐标展开（沿孔壁环形展开成直角坐标条带）
         → 切向方向高斯平滑（σ=10 像素）消除缓慢变化的壁纹理
         → 残差 = max(切向背景 - 展开图像, 0)
         → 在残差图上检测局部最大值 → 紧凑物体候选
```

这样，孔壁变成了缓慢变化的水平纹理，而细胞是一个局部中断，减除后可以被检测到。

#### 3.1.5 壁链检测

识别沿孔壁边缘排列的伪影链（光学伪影通常沿壁形成连续弧线）：
- 在壁邻域（r ≥ 0.40）内用 cKDTree 搜索每个点的邻居
- 统计同半径层（径向差 ≤ 0.025）且距离 ≤ 110px 的邻居数
- ≥ 3 个邻居 → 标记为"壁链"，后续优先级降低

---

### 3.2 伪标签生成：`pseudo_labels.py` — 形态学规则引擎

在没有人工标注的情况下，用基于规则的启发式方法生成初始训练标签（细胞 vs 碎片）。

#### 3.2.1 特征提取

从 CF 仪器掩膜的连通分量提取 **6 个形态学特征**：

| 特征 | 含义 | 细胞期望范围 |
|------|------|------------|
| area_px | 面积（像素） | 12 ~ 800 |
| circularity | 圆度 (4πA/P²) | ≥ 0.52 |
| eccentricity | 偏心率 | ≤ 0.96 |
| solidity | 凸包面积比 | ≥ 0.74 |
| extent | 外接矩形占比 | ≥ 0.38 |
| radial_fraction | 径向位置 | < 0.45 |

#### 3.2.2 额外证据

- **时间支持** (temporal_support)：同一位置（≤ 24px）在其他时间点也检测到 → 0~2
- **背景各向异性** (background_anisotropy)：候选周围背景的梯度结构张量各向异性 → < 0.25 为细胞
- **壁邻居计数** (wall_neighbor_count)：≥ 2 可能为壁链

#### 3.2.3 分类规则

```
高置信度细胞 = 形状符合 + 时间支持≥1 + 不在壁上 + 背景各向异性≤0.25
高置信度非细胞 = 明显异常形状 OR 方向性壁链
其他 = uncertain
```

置信度公式：
```
细胞置信度 = 0.65 + 0.10 × temporal_support + 0.10 × circularity
非细胞置信度 = 0.72 + 0.08 × obvious_noncell + 0.08 × wall_chain
```

#### 3.2.4 平衡采样

- 每孔每类最多 220 个样本
- 全局每类最多 12000 个样本
- 类间平衡

---

### 3.3 层次抑制：`hierarchy.py` — 跨源去重整合

候选来自多个来源（CF 分量、原始图像峰值、仪器 CSV、手动标注），需要去重整合。

#### 3.3.1 来源优先级

```
manual_cell_anchor (6) > instrument_csv (5) > cf_component (4)
> wall_residual/wall_rescue (3) > multiscale_dense_peak (2)
```

#### 3.3.2 自适应距离去重

不同标签类型的去重距离不同：
- **单细胞 vs 单细胞**：最多合并两个距离 ≤ 0.42×直径 + 跨源奖励的候选
- **多细胞 vs 多细胞**：合并距离 ≤ 0.60×较大直径的候选
- **碎片 vs 碎片**：合并距离 ≤ 0.45×较小直径的候选

#### 3.3.3 嵌套抑制

一个团簇/双细胞的轮廓可能包含其内部的单细胞候选核心：
```
if single_cell ∈ doublet/cluster footprint:
    single.is_hierarchy_suppressed = True  # 该单细胞不计入独立计数
    single.parent_candidate_id = group.id  # 但保留关联，供审核使用
```

#### 3.3.4 CF 足迹关联

密集检测器只报告局部最大值，不是物体边界。系统将附近的密集峰值关联到最近的 CF 分量（作为完整对象掩膜的代理）。

---

### 3.4 V1：弱分割器 TinyUNet

**目的**：在仪器 CF 掩膜（弱标签）上训练一个轻量级分割网络，学习区分前景和背景。

```
架构: 3级编码器 U-Net
输入: 1 通道 96×96 灰度块
编码: ConvBlock(1→8) → Pool → ConvBlock(8→16) → Pool → ConvBlock(16→32)
瓶颈: ConvBlock(32→64)
解码: 对称上采样 + 跳连
输出: 1 通道前景 logit

损失: Dice Loss + BCE Loss
```

这个 V1 模型是 V2 模型的垫脚石，提供了初始的候选分类能力。

---

### 3.5 V2 核心模型

#### 3.5.1 SeededInstanceUNet — 种子条件化实例分割

这是 V2 的核心创新，解决了 **相邻细胞难以分离** 的核心问题。

```
架构: 4级编码器 U-Net (base_channels=16)
输入: 3 通道 96×96 块
  - 通道 0: 归一化原始图像 (2%-98% 百分位归一化)
  - 通道 1: 高斯种子热图 (以候选点为中心, σ 可配)
  - 通道 2: 几何壁先验 (距孔壁距离的梯度信息)

编码: 16→32→64→128 (4层, 每层有 MaxPool)
瓶颈: ConvBlock(128→256)
解码: 对称上采样 (ConvTranspose2d) + 跳连

输出: 3 通道
  - 通道 0: 实例掩膜 (当前选中物体的像素级分割)
  - 通道 1: 物理孔壁掩膜 (标记哪些像素属于孔壁结构)
  - 通道 2: 物体存在概率图 (判断此候选是否真的有物体)
```

**设计思路**：
- 传统的语义分割只能输出"哪里是细胞"，但无法区分相邻细胞。通过在种子位置给出高斯热图作为条件，模型学会了"分割离这个种子最近的物体"。
- 同时预测壁掩膜，让模型学会区分细胞轮廓和孔壁边缘。
- 物体存在概率图作为一个质量门控，过滤假候选。

#### 3.5.2 TemporalEvidenceNet — 时间证据网络

利用同一孔在 T0/T1/T2 三个时间点拍摄的图像，判断一个候选是**活细胞**（会移动/变化）还是**静态碎片**（位置/形态不变）。

```
架构:
  图像编码器: 
    输入: 2通道(T0/T1/T2每个时间点的 [原始图像, 分割掩膜])
    Conv2d(2→24) → Conv2d(24→48) → Conv2d(48→72)
    → AdaptiveAvgPool → Linear(72→96) → 96维嵌入
    
  每个时间点独立编码 → 3个96维嵌入
  
  融合层:
    输入 = [嵌入T0, 嵌入T1, 嵌入T2, 12维数值特征, 3维存在性标志]
    数值特征包括:
      - T0→T1, T1→T2, T0→T2 的位移向量 (6维)
      - T0→T1, T1→T2 的面积变化对数比 (2维)
      - 最大面积/500 (1维)
      - T0/T1/T2 的多重性编码 (3维)
      - 各时间点的细胞/碎片/无效/壁重叠概率 (12维)
    
    → Linear(96×3+12+3→192) → SiLU → Dropout(0.2) 
    → Linear(192→96) → SiLU
    
  两个输出头:
    - same_object logit: 三帧是否是同一物体
    - static_similarity logit: 三帧中物体形态有多相似（越相似越可能是碎片）
```

**概率调整逻辑**：
```
1. 如果 invalid_prob ≥ 0.60 → 不调整
2. 如果 same_object < 阈值 → 不调整 (可能不是同一物体)
3. 如果 static_similarity < 阈值 → 不调整 (物体有变化，可能是活细胞)
4. 如果条件细胞概率已经很确定 (≥0.90) → 不调整
5. 否则：按 static_similarity 和 ambiguous 程度计算碎片概率的增量
   shift = β × same_object_score × static_excess × ambiguity × frame_factor
   cell_prob -= shift, debris_prob += shift (最多调整 0.30)
```

---

### 3.6 V2 推断管道

#### 3.6.1 实例分割推断 (`v2_instance_inference.py`)

```
对每个候选:
  1. 裁剪 96×96 块
  2. 构建 3 通道输入 (归一化原始图像 + 种子热图 + 壁先验)
  3. 模型前向 → 3 通道输出
  4. 阈值化：object_prob ≥ threshold, 排除高置信壁像素
  5. 选择离中心最近的连通分量
  6. 掩膜细化 (测地线主动轮廓)：
     - 用 morphological_geodesic_active_contour 将掩膜边界吸附到图像边缘
     - 约束：不允许膨胀到高置信壁像素上 (wall_prob ≥ 0.92)
     - 约束：总膨胀限制在原始掩膜的 3 像素膨胀范围内
  7. 壁拒绝：如果壁重叠 > 86% 且 mask 细长 且 置信度 < 88% → 拒绝
  8. 轮廓提取：在 4× 超分辨率概率图上用 marching squares，光滑后降采样
```

#### 3.6.2 掩膜合并 (`consolidate_v2_masks`)

对同一孔同一时间点内的所有有效掩膜：
- 按标签优先级排序（团簇 > 双细胞 > 单细胞 > 碎片 > 不确定 > 无效）
- 计算全局 IoU 和包含率
- 如果 IoU ≥ 阈值 或 包含率 ≥ 65% → 保留更优者，抑制另一个
- 为每个保留的实例分配唯一 ID

#### 3.6.3 最终决策 (`finalize_v2_instances`)

```
if invalid_prob ≥ 0.60 且 非人工保护 → label = "invalid"

if mask_valid 且 非 wall_rejected 且 非 auto_invalid:
    if v2_instance_confidence ≥ 0.72:
        if cell_prob ≥ 0.28 → 根据 multiplicity 设为 cell 类型
        elif debris_prob ≥ 0.55 → label = "debris"
        else → label = "uncertain"

if cell_prob < 0.30 且 label ∈ {"uncertain", "unmarked"}:
    if debris_prob ≥ invalid_prob → label = "debris"
    else → label = "invalid"
```

#### 3.6.4 时间证据推断 (`v2_temporal_inference.py`)

```
对每个孔：
  对每个 temporal_candidate (T0 锚点)：
    1. 在 T1/T2 中找到最近的同孔候选 (距离 ≤ 128px)
    2. 构建三帧输入 (原始图像 + 掩膜)
    3. 构建数值特征 (位移、面积比、多重性、形态学概率)
    4. TemporalEvidenceNet 前向 → same_object_score, static_similarity_score
    5. 调整细胞/碎片概率
    6. 静态壁伪影检测：
       如果三帧都存在、same > 阈值、static > 阈值、位移 < 5px、
       面积比 < 2.2、壁重叠 > 90% → 标记为 "static_wall_artifact" → label = "invalid"
    7. 低细胞非细胞解析：
       如果 cell_prob < 0.30 且 label in {"uncertain", "unmarked"}：
         如果 debris_prob ≥ invalid_prob → label = "debris"
         否则 → label = "invalid"
```

---

### 3.7 时间追踪与谱系

#### 3.7.1 匈牙利匹配 (`tracking.py`)

```
T0 → T1, T1 → T2 的一对一最优匹配：
  代价 = 欧氏距离 + 0.1 × |log(面积比)|
  用 linear_sum_assignment (匈牙利算法) 求最小代价匹配
  分裂候选检测：父细胞面积在 [0.5×子细胞面积和, 2.5×子细胞面积和]
```

#### 3.7.2 谱系审核 (`lineage_engine.py`)

规范化的 T0/T1/T2 跨时间点目标追踪，管理已审核的谱系，触发时间补全。

#### 3.7.3 时间补全 (`temporal_completion.py`)

用高置信度锚点进行前向/后向搜索，补全缺失时间点的检测：
- 锚点要求：cell_probability ≥ 0.82，integrated_confidence ≥ 0.82
- 多半径搜索：[96, 192, 384, 768] 像素
- 自动提升阈值：0.78 → 自动接受
- 审核阈值：0.58 → 加入审核队列

---

### 3.8 辅助分类器

#### 3.8.1 MorphologyClassifier — 形态学二分类器

```
架构: 小型 CNN (16→32→64→96 通道)
输入: 1 通道灰度块 (96×96)
输出: 2 类 (碎片/伪影 vs 细胞)

训练数据: 高置信度伪标签 (每类最多 12000, 每孔每类 ≤ 220)
排除: A1 孔 (密集阳性对照)
```

#### 3.8.2 教学分类器 (`teaching.py`)

基于 ResNet-18 特征提取器 + KNN 混合：
- 3 类输出：无效 / 碎片 / 细胞
- 时间点平衡 (timepoint_knn_blend = 0.40)
- 自动标注轮次生成
- 用于自动预标注以减少人工审核量

#### 3.8.3 多细胞分类器 (`multiplicity.py`)

区分单个细胞 vs 接触双细胞 vs 团簇 (3+ 细胞)：
- 自动阈值 0.425
- 多尺度搜索 [160, 320, 640, 1200] 像素
- 用于正确的实例计数

---

### 3.9 孔级筛选 (`well_screening.py`)

汇总所有 T0-T2 时间点的自动标注结果，做出孔级生长决策：

输出三类：
- **活跃** (active)：检测到明显细胞生长
- **不活跃** (inactive)：无明显细胞活动
- **模糊** (uncertain)：需要人工判断

#### 晚期生长推断 (`late_growth_inference.py`)

专门处理 T3/T4 晚期时间点（D14）的密集生长判断：
- 分阶段搜索：先在高置信 T0-T2 锚点附近搜索（450px 半径，160 候选）
- 再扩大到 850px 半径，280 候选
- 最后全孔搜索，480 候选
- 生长判定：新增细胞数 ≥ 2 且 增长率 ≥ 1.5×
- 密集模式判定：用 384×384 tile 的前景占比变化来检测连续菌落

---

### 3.10 人机协同审核系统

基于 FastAPI 的 Web 审核服务器（localhost:8765），包含多个专用页面：

| 页面 | 功能 |
|------|------|
| **教学标注** (teach.html) | 人工标注细胞/碎片/无效，用于训练教学分类器 |
| **自动审核** (auto-review.html) | 审核自动标注结果，修正错误 |
| **双细胞教学** (doublet-teach.html) | 专门标注接触双细胞 |
| **集成审核** (integrated-review.html) | 快速审核仪表板，审核 V2 模型的输出 |
| **孔级筛选** (screening.html) | 孔级生长决策审核 |

SQLite 数据库持久化所有审核结果，后续训练自动使用人工修正。

---

## 四、关键设计理念总结

### 4.1 从弱到强的渐进式训练

```
CF仪器掩膜(弱标签) → V1 TinyUNet → 伪标签规则 → MorphologyClassifier
                                                    ↓
人工审核数据(强标签) → V2 SeededInstanceUNet + TemporalEvidenceNet
```

不是一步到位，而是逐步积累高质量标签。

### 4.2 空间-时间联合推理

- **空间维度**：种子条件化实例分割 → 区分离得很近的不同细胞
- **时间维度**：三帧联合学习 → 利用"活细胞会动/碎片不动"的生物学先验

### 4.3 孔壁感知设计

整个管道中孔壁是一个贯穿始终的一等公民：
- 动态壁检测（每张图单独计算壁位置）
- 壁缓冲区和壁救援区的差异化处理
- 极坐标壁残差检测（创新性的壁重叠细胞检测方案）
- 壁链识别（区分真实的壁贴壁细胞和光学伪影弧线）
- 壁先验通道（作为神经网络输入条件）
- 壁拒绝规则（掩膜细化时不允许膨胀到壁上）

### 4.4 多层次去重与抑制

```
跨源去重 → 嵌套抑制 → 掩膜IoU合并 → 时间证据确认
```

每个层次解决不同的问题，层层递进。

---

## 五、模型架构一览

```
┌─────────────────────────────────────────────────────────┐
│                    V1: TinyUNet                          │
│  输入: 1ch 96×96 │ 编码器: 8→16→32 │ 输出: 1ch logit    │
│  用途: 弱监督前景分割                                     │
├─────────────────────────────────────────────────────────┤
│                    MorphologyClassifier                  │
│  输入: 1ch 96×96 │ 编码: 16→32→64→96 │ 输出: 2类        │
│  用途: 碎片/细胞二分类                                    │
├─────────────────────────────────────────────────────────┤
│              V2: SeededInstanceUNet                      │
│  输入: 3ch 96×96 (图像+种子热图+壁先验)                   │
│  编码器: 16→32→64→128 │ 输出: 3ch (实例+壁+存在)         │
│  用途: 种子条件化实例分割                                 │
├─────────────────────────────────────────────────────────┤
│               V2: TemporalEvidenceNet                    │
│  输入: 三帧 2ch 块 + 数值特征 + 存在性                     │
│  编码: 2→24→48→72→96 嵌入 │ 融合: 192→96                 │
│  输出: same_object + static_similarity logits            │
│  用途: 时间证据学习 (活细胞 vs 静态碎片)                   │
├─────────────────────────────────────────────────────────┤
│               辅助: Teaching (ResNet-18)                  │
│  输入: 裁剪块 │ 特征提取: ResNet-18 backbone              │
│  输出: 3类 (无效/碎片/细胞) + KNN混合                     │
│  用途: 自动预标注                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 六、配置文件说明

系统通过 YAML 配置文件驱动（`configs/default.yaml`），支持继承（`base_config`），主要参数类别：

- **calibration**：分辨率校准（2.08 μm/px）
- **candidate_filter**：壁过滤参数
- **hierarchical_suppression**：去重抑制参数
- **dense_detection**：密集候选检测参数
- **morphology_classifier**：形态学分类器训练参数
- **review_queue**：审核队列参数
- **teaching**：教学分类器参数
- **auto_annotation**：自动标注阈值
- **temporal_completion**：时间补全参数
- **multiplicity**：多细胞分类参数
- **late_growth**：晚期生长检测参数

---

## 七、测试数据与外部验证

- **训练数据**：QL11111 (96孔板)、QL2202（验证板）
- **外部验证**：A12-22 板（显式排除在训练外，仅用于外部验证）
- **各板数据量**：约 788 张 TIFF 原始图像 + 464 个 CSV 文件/板
- **测试覆盖**：21 个 pytest 测试文件覆盖所有核心模块
